# OpenAPI / Swagger

1. Запустите API из каталога `backend`: `python app.py` (или `uvicorn app:app --reload --port 8000`).
2. Swagger UI: `http://127.0.0.1:8000/docs`
3. ReDoc: `http://127.0.0.1:8000/redoc`

Порт задаётся переменной `PORT` в `backend/.env` (по умолчанию 8000).
