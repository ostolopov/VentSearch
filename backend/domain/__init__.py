"""
Доменный слой (Domain Layer) — центр луковицы.

Содержит:
- Сущности (Entities): Product, User — чистые бизнес-объекты без зависимостей на БД/HTTP.
- Интерфейсы (Interfaces/Ports): абстрактные репозитории, которые реализует инфраструктурный слой.
- Доменные сервисы (Domain Services): бизнес-логика, не принадлежащая ни одной сущности (Q-P кривые).

Зависимости: только стандартная библиотека Python. Никаких импортов FastAPI / psycopg2 / bcrypt.
"""
