diff --git a/PRESENTATION_CHECKLIST.txt b/PRESENTATION_CHECKLIST.txt
new file mode 100644
index 0000000..21c57e2
--- /dev/null
+++ b/PRESENTATION_CHECKLIST.txt
@@ -0,0 +1,99 @@
+===========================================================================
+ VENTSEARCH — ЧЕК-ЛИСТ ПЕРЕД ПРЕЗЕНТАЦИЕЙ / УСТАНОВКОЙ НА СЕРВЕР
+===========================================================================
+Пройдите список сверху вниз. Пункты со звёздочкой (*) обязательны для
+установки на реальный сервер (безопасность). Остальное — «чтобы не стыдно
+было показать».
+
+---------------------------------------------------------------------------
+1. БЕЗОПАСНОСТЬ (secrets\.env.local) — ОБЯЗАТЕЛЬНО
+---------------------------------------------------------------------------
+* JWT_SECRET       — заменить "change-me-in-production" на длинную случайную
+                     строку (иначе токены входа можно подделать).
+* ADMIN_PASSWORD   — заменить дефолтный "admin123" на свой пароль.
+* ADMIN_EMAIL      — поставить рабочий e-mail администратора.
+* POSTGRES_PASSWORD— задать надёжный пароль БД (и синхронно в DATABASE_URL).
+* CORS_ORIGINS     — указать реальный домен сайта, убрать localhost:5500.
+
+  Проверить, что дефолты не остались, можно в админ-панели → «Диагностика»:
+  строки «JWT-секрет по умолчанию» и «Пароль админа по умолчанию» должны
+  быть «нет» (не красные).
+
+---------------------------------------------------------------------------
+2. РЕЖИМ ПОКАЗА (DEMO_MODE в secrets\.env.local)
+---------------------------------------------------------------------------
+- DEMO_MODE=1 — «инсайдерский» показ: каталожные данные (цены/характеристики)
+  скрыты серой плашкой «Недоступно в инсайдерской версии». Ставьте, если
+  показываете интерфейс людям, которым нельзя видеть реальные данные.
+- DEMO_MODE=0 (или строки нет) — всё видно. Ставьте для реальной работы.
+
+---------------------------------------------------------------------------
+3. РАЗДЕЛЫ, НАМЕРЕННО ЗАКРЫТЫЕ («в разработке»)
+---------------------------------------------------------------------------
+- Вкладка «Доставка» залита серым с красной надписью ВСЕГДА (заглушка).
+  Открыть, когда раздел будет готов: убрать атрибут data-locked-section
+  у блока <div class="row g-4" ...> в frontend/delivery.html.
+- Вкладка «Проект для клиента» («Мой проект») сейчас оформлена как
+  клиентская спецификация. Если нужен отдельный кабинет сотрудника —
+  это отдельная доработка (см. TROUBLESHOOTING.txt / раздел «Проект»).
+
+---------------------------------------------------------------------------
+4. КАРТИНКИ И БРЕНДИНГ (папка photos\)
+---------------------------------------------------------------------------
+- logo.png             — логотип в шапке сайта.
+- ВЕНТМАШ_ЛОГО.png     — водяной знак PDF по умолчанию (кладите сюда; он сам
+                         выберется в окне «Просмотр PDF»).
+- Фото по типам        — vo.jpeg / vc.jpeg / vr.jpeg / vkop.jpeg / uvo.jpeg /
+                         c.jpeg (показываются в каталоге и карточке).
+- Чертежи:
+    blueprint.png        — общий чертёж (или blueprint_<слаг-модели>.png);
+    blueprintVals.png    — таблица значений (или blueprintVals_<слаг>.png).
+  Пока файлов нет — на карточке и в PDF показываются SVG-заглушки «синьки».
+- Иконки сайта (favicon): положить квадратный логотип в
+  photos\favicon_template.png и выполнить:
+      pip install pillow
+      python scripts\generate_favicons.py
+
+---------------------------------------------------------------------------
+5. ДАННЫЕ КАТАЛОГА
+---------------------------------------------------------------------------
+- Актуальный CSV: data\ventsearch_massive_sorted.csv (формат имён
+  «ВО 13-284-4/15°-4-56A4», типоразмер = маркировка двигателя, напр. 56A4).
+- Резервные/тестовые CSV в data\old\ на сервер не нужны — можно удалить.
+- После правки CSV сервер сам перечитает его (по mtime/размеру); при
+  необходимости — админ-панель → «Диагностика» → «Перечитать CSV сейчас».
+
+---------------------------------------------------------------------------
+6. АНАЛИТИКА / ВНЕШНИЕ СЧЁТЧИКИ
+---------------------------------------------------------------------------
+- На всех страницах вставлен счётчик Яндекс.Метрики (id 109006784).
+  Если презентация не должна слать данные наружу — временно удалите блок
+  <!-- Yandex.Metrika counter --> ... <!-- /Yandex.Metrika counter -->
+  из frontend\*.html (или замените id на свой).
+
+---------------------------------------------------------------------------
+7. ЗАПУСК
+---------------------------------------------------------------------------
+- База данных (Postgres) поднимается в Docker:
+      docker compose -f deploy\docker-compose.dev.yml up -d
+- Сайт (backend + фронт) — БЕЗ Docker:
+      deploy\run.bat
+- Открывать в браузере ТОЛЬКО:  http://127.0.0.1:8000/
+  (uvicorn пишет «0.0.0.0:8000» — это не адрес для браузера, а «все
+  сетевые адреса»; см. TROUBLESHOOTING.txt).
+- Для реального сервера: запускать за обратным прокси (nginx) с HTTPS,
+  а не отдавать uvicorn напрямую в интернет.
+
+---------------------------------------------------------------------------
+8. БЫСТРАЯ ПРОВЕРКА ПЕРЕД ПОКАЗОМ
+---------------------------------------------------------------------------
+[ ] curl http://127.0.0.1:8000/api/health -> {"ok":true, ...}
+[ ] Каталог открывается, таблица с моделями видна.
+[ ] Открыть карточку модели: график рисуется, чертёж/значения на месте.
+[ ] «Просмотр PDF» на сравнении/карточке/проекте — PDF формируется,
+    водяной знак = ваш логотип.
+[ ] Войти админом -> «⚙ Админ-панель» в шапке -> вкладки открываются,
+    «Диагностика» показывает зелёные проверки безопасности.
+[ ] favicon виден на вкладке браузера (если нет — Ctrl+Shift+R).
+
+Подробности по типовым ошибкам запуска — в TROUBLESHOOTING.txt.
diff --git a/TROUBLESHOOTING.txt b/TROUBLESHOOTING.txt
new file mode 100644
index 0000000..b3539b8
--- /dev/null
+++ b/TROUBLESHOOTING.txt
@@ -0,0 +1,175 @@
+# Запуск и типовые проблемы (шпаргалка)
+
+Короткая справка по командам и ошибкам, с которыми реально сталкивались при
+локальном запуске на Windows. Для первого знакомства с проектом — см.
+[docs/INSTALL_LOCAL.md](docs/INSTALL_LOCAL.md). Этот файл — «скорая помощь»,
+когда что-то уже не работает.
+
+## Как всё устроено
+
+- **PostgreSQL** запускается в Docker: `deploy/docker-compose.dev.yml`.
+- **Backend** (FastAPI) запускается НЕ в Docker — обычным Python-процессом
+  через `run.bat`. Docker отвечает только за базу данных.
+- **Frontend** — статические файлы, которые раздаёт сам backend
+  (`http://localhost:8000`).
+
+Из этого следует важное: если сломалось что-то в самом сайте (не в базе) —
+перезапуск Docker чаще всего ничего не даст, потому что backend и frontend
+в Docker вообще не живут.
+
+## Команды на каждый день
+
+```bat
+:: поднять базу данных (один раз после перезагрузки компьютера)
+docker compose -f deploy\docker-compose.dev.yml up -d
+
+:: проверить, что база жива
+docker compose -f deploy\docker-compose.dev.yml ps
+
+:: запустить сайт
+run.bat
+
+:: проверить, что backend отвечает
+curl http://localhost:8000/api/health
+```
+
+Ожидаемый ответ `/api/health`: `{"ok":true,"products":ЧИСЛО,"demo_mode":false}`.
+
+## Частые ошибки
+
+### 1. `Connection refused` на порту 5432
+
+Postgres не запущен. `run.bat` его не поднимает — это делает только
+`docker-compose.dev.yml`. Решение:
+
+```bat
+docker compose -f deploy\docker-compose.dev.yml up -d
+```
+
+### 2. Сайт не пускает / странные ошибки после правки `secrets\.env.local`
+
+Проверьте, что `DATABASE_URL` и `POSTGRES_USER` / `POSTGRES_PASSWORD` /
+`POSTGRES_DB` **согласованы** — это одна и та же база, названная дважды.
+Если один блок правили, а другой нет, backend будет пытаться подключиться
+не туда:
+
+```
+POSTGRES_USER=ventmash
+POSTGRES_PASSWORD=ventpass
+POSTGRES_DB=ventmash
+DATABASE_URL=postgresql://ventmash:ventpass@localhost:5432/ventmash
+```
+
+Логин и пароль в `DATABASE_URL` должны буквально совпадать с `POSTGRES_USER`
+/ `POSTGRES_PASSWORD` выше.
+
+### 3. Ошибки то есть, то нет; `/api/products/facets` 500; не получается войти — хотя всё вроде запущено
+
+Почти всегда это **не новая поломка**, а старый процесс backend, который
+не завершился при предыдущей остановке и продолжает висеть на порту 8000
+рядом с новым. Браузер стучится то в старый (сломанный) процесс, то в
+новый — отсюда непредсказуемость.
+
+Проверить, кто сидит на порту:
+
+```bat
+netstat -ano | findstr :8000
+```
+
+Если строк больше одной (кроме `TIME_WAIT`) — это и есть проблема. Завершить
+процесс по PID:
+
+```bat
+taskkill /F /PID <тот_самый_PID>
+```
+
+### 4. `taskkill` пишет «не может найти процесс», а `netstat` всё равно показывает порт занятым
+
+Это не баг сайта — это осиротевший сокет от сетевого слоя Docker Desktop
+(WSL2), который держит порт, но уже не привязан ни к одному живому
+Windows-процессу. Обычный `taskkill` его не видит. Решение по нарастающей:
+
+```bat
+wsl --shutdown
+```
+
+затем перезапустить Docker Desktop. Если не помогло — перезагрузка
+компьютера гарантированно освобождает порт.
+
+### 5. Заменили файл (фото, favicon), а на сайте всё равно старая версия
+
+Это не Docker и не backend — файл на диске уже новый, backend отдаёт его
+сразу. Дело в **кэше браузера**: URL картинки не меняется при замене файла,
+и браузер показывает то, что закэшировал раньше. Решение:
+
+- жёсткое обновление страницы (`Ctrl+Shift+R`) или окно инкогнито;
+- для `photos/*.jpg|png` кэш сбивается автоматически (добавляется
+  `?v=<время изменения файла>`) — если после правки фото всё равно старое,
+  проверьте вкладку Network в DevTools: грузится ли новый `?v=...`;
+- у favicon.ico браузеры кэшируют иконку особенно агрессивно и надолго —
+  иногда помогает только очистка данных сайта в настройках браузера;
+- важно: раньше сервер вообще не отдавал favicon-файлы (белый список
+  статики их не включал, всегда был 404) — если иконки нет, убедитесь,
+  что backend обновлён, и проверьте `curl http://localhost:8000/favicon.ico`
+  (должен быть ответ 200, а не 404).
+
+### 6. Не появляется серая плашка «Недоступно в инсайдерской версии»
+
+Проверьте `secrets\.env.local` — там должна быть строка `DEMO_MODE=1` (без
+неё режим выключен, и все данные видны как обычно — это нормальное
+поведение, не баг). Если строка есть и всё равно не работает — см. пункт 5
+(кэш браузера) и пункт 3 (лишний процесс backend).
+
+## Свои иконки сайта (favicon)
+
+Иконки генерируются из одного файла-шаблона скриптом, а не редактируются
+руками по отдельности:
+
+```bash
+pip install pillow
+python scripts/generate_favicons.py
+```
+
+Положите свой логотип (квадратный, от 512×512, лучше PNG с прозрачным
+фоном) в `photos/favicon_template.png` и запустите скрипт — он пересоздаст
+`favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`,
+`apple-touch-icon.png` в `frontend/`.
+
+## Водяной знак и заголовок в PDF («Документ для клиента»)
+
+Кнопки «Документ для клиента (PDF)» / «PDF для клиента» есть на странице
+сравнения, на карточке модели и в «Моём проекте» — все они открывают одно
+и то же окно: своё название шапки документа (если оставить пустым —
+остаётся текст по умолчанию), водяной знак, предпросмотр и скачивание.
+Водяной знак — любой файл-картинка из папки `photos/`. Чтобы добавить свой
+вариант, просто положите файл (`.png`/`.jpg`/`.webp`/`.gif`) в `photos/` —
+он сам появится в выпадающем списке.
+
+## Чертежи из каталога на карточке модели
+
+Блок «Чертёж из каталога» показывает ДВЕ картинки рядом: слева сам чертёж,
+справа — таблицу подробных значений. Файлы кладутся в `photos/`:
+
+- левая (чертёж): `blueprint_<слаг-модели>.png` или общий `blueprint.png`;
+- правая (значения): `blueprintVals_<слаг-модели>.png` или общий
+  `blueprintVals.png`.
+
+Подойдут расширения `.png`/`.jpg`/`.jpeg`/`.webp`. Сначала ищется вариант с
+`_<слаг>`, потом общий файл без слага, потом заглушка (`frontend/img/…`).
+Точные ожидаемые имена для текущей модели видны в подсказке под блоком
+(только у администратора). Клик по любой картинке открывает её на весь экран.
+Эти же изображения попадают в PDF-документ для клиента.
+
+## Раздел «Доставка» временно закрыт
+
+Страница доставки намеренно залита серым с красной надписью «Недоступно в
+инсайдерской версии» — независимо от `DEMO_MODE`. Это заглушка «в разработке».
+Чтобы снова открыть раздел, уберите в `frontend/delivery.html` атрибут
+`data-locked-section` у блока `<div class="row g-4" ...>`.
+
+## Сортировка каталога
+
+Каталог — таблица: клик по заголовку колонки (Расход, Давление, Мощность,
+Шум, Цена, Диаметр, Модель) сортирует весь каталог по этой колонке,
+повторный клик — в обратную сторону. Сортируется на сервере, поэтому
+порядок честный по всем страницам, а не только по видимой.
diff --git a/backend/presentation/app.py b/backend/presentation/app.py
index d8d7275..1d970d4 100644
--- a/backend/presentation/app.py
+++ b/backend/presentation/app.py
@@ -509,53 +509,48 @@ def _build_compare_pdf(
                 pdf.drawString(left + 3 * mm, y - (29.5 + i * 4) * mm, dl)
         y -= card_h + 3.5 * mm
 
-    # Чертежи из каталога: слева сам чертёж (blueprint), справа — таблица
-    # значений (blueprintVals), если файл добавлен. Без файлов раздел не
-    # печатается. Модели без чертежа пропускаются.
-    def _fit(reader, box_w, box_h):
-        iw, ih = reader.getSize()
-        s = min(box_w / iw, box_h / ih)
-        return iw * s, ih * s
-
-    for p in products:
-        bp_path = _find_product_blueprint(p)
-        vals_path = _find_product_blueprint_vals(p)
-        if not bp_path and not vals_path:
-            continue
+    # Чертёж из каталога печатаем ОДИН РАЗ (общий для ряда), крупно, каждое
+    # изображение с новой строки во всю ширину: сначала сам чертёж
+    # (blueprint), ниже — таблица значений (blueprintVals). Одинаковые для
+    # разных моделей файлы не дублируем.
+    def _draw_full_width_image(path, title):
+        nonlocal y
         try:
-            block_h = 96 * mm
-            if y - (block_h + 8 * mm) < 12 * mm:
+            img = ImageReader(str(path))
+            iw, ih = img.getSize()
+            draw_w = width
+            draw_h = draw_w * ih / iw
+            max_h = 210 * mm
+            if draw_h > max_h:
+                draw_h = max_h
+                draw_w = draw_h * iw / ih
+            if y - (draw_h + 10 * mm) < 12 * mm:
                 new_page()
-            line(f"Чертёж: {_normalize_ws(p.get('model') or '—')}", step=6.5 * mm, bold=True, size=10)
-            gap = 4 * mm
-            two = bool(bp_path and vals_path)
-            left_w = (width * 0.6 - gap / 2) if two else width
-            right_w = (width * 0.4 - gap / 2) if two else 0
-            top_y = y
+            line(title, step=6.5 * mm, bold=True, size=10)
             pdf.setStrokeColor(c_border)
             pdf.setFillColor(colors.white)
-            if bp_path:
-                pdf.roundRect(left, top_y - block_h, left_w, block_h, 2 * mm, stroke=1, fill=1)
-                dw, dh = _fit(ImageReader(str(bp_path)), left_w - 4 * mm, block_h - 4 * mm)
-                pdf.drawImage(
-                    ImageReader(str(bp_path)),
-                    left + (left_w - dw) / 2, top_y - (block_h + dh) / 2,
-                    width=dw, height=dh, preserveAspectRatio=True, mask="auto",
-                )
-            if vals_path:
-                rx = left + (left_w + gap if bp_path else 0)
-                rw = right_w if bp_path else width
-                pdf.roundRect(rx, top_y - block_h, rw, block_h, 2 * mm, stroke=1, fill=1)
-                dw, dh = _fit(ImageReader(str(vals_path)), rw - 4 * mm, block_h - 4 * mm)
-                pdf.drawImage(
-                    ImageReader(str(vals_path)),
-                    rx + (rw - dw) / 2, top_y - (block_h + dh) / 2,
-                    width=dw, height=dh, preserveAspectRatio=True, mask="auto",
-                )
-            y -= block_h + 8 * mm
+            pdf.roundRect(left, y - draw_h - 3 * mm, width, draw_h + 3 * mm, 2 * mm, stroke=1, fill=1)
+            pdf.drawImage(
+                img, left + (width - draw_w) / 2, y - draw_h - 1.5 * mm,
+                width=draw_w, height=draw_h, preserveAspectRatio=True, mask="auto",
+            )
+            y -= draw_h + 8 * mm
         except Exception:
             pass
 
+    seen_bp: set[str] = set()
+    for p in products:
+        bp_path = _find_product_blueprint(p)
+        vals_path = _find_product_blueprint_vals(p)
+        key = f"{bp_path}|{vals_path}"
+        if (not bp_path and not vals_path) or key in seen_bp:
+            continue
+        seen_bp.add(key)
+        if bp_path:
+            _draw_full_width_image(bp_path, "Чертёж из каталога")
+        if vals_path:
+            _draw_full_width_image(vals_path, "Таблица присоединительных размеров")
+
     pdf.save()
     data = buf.getvalue()
     buf.close()
diff --git a/deploy/run.bat b/deploy/run.bat
index 4e3b49c..b698fe5 100644
--- a/deploy/run.bat
+++ b/deploy/run.bat
@@ -1,4 +1,6 @@
 @echo off
+rem Кодовая страница UTF-8, иначе кириллица в консоли превращается в кракозябры
+chcp 65001 >nul
 rem Запуск API локально без Docker (Windows CMD).
 rem Запуск из корня репозитория: deploy\run.bat
 setlocal
@@ -51,12 +53,19 @@ for /f "usebackq delims=" %%I in (`powershell -NoProfile -Command "$ip=(Get-NetI
 )
 
 echo.
-echo Секреты: secrets\.env.local
-echo API:     http://127.0.0.1:8000/docs
+echo ============================================================
+echo  Открывайте сайт в браузере:   http://127.0.0.1:8000/
+echo  Документация API (Swagger):   http://127.0.0.1:8000/docs
 if defined LAN_IP (
-  echo Сеть:    http://%LAN_IP%:8000/
+  echo  Для других устройств в сети:  http://%LAN_IP%:8000/
 )
 echo.
+echo  ВНИМАНИЕ: ниже uvicorn напишет "Running on http://0.0.0.0:8000".
+echo  0.0.0.0 означает "на всех сетевых адресах" — это НЕ адрес для
+echo  браузера. Открывайте именно http://127.0.0.1:8000/
+echo  Секреты берутся из secrets\.env.local
+echo ============================================================
+echo.
 
 "%VPY%" app.py
 exit /b %ERRORLEVEL%
diff --git a/frontend/404.html b/frontend/404.html
index e0beeca..46c1af2 100644
--- a/frontend/404.html
+++ b/frontend/404.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
   </head>
   <body data-page="error">
     <nav class="navbar navbar-expand-lg sticky-top">
@@ -99,7 +99,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-9"></script>
     <script src="site-auth.js?v=20260524-9"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/500.html b/frontend/500.html
index 2907d33..9176e8f 100644
--- a/frontend/500.html
+++ b/frontend/500.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
   </head>
   <body data-page="error">
     <nav class="navbar navbar-expand-lg sticky-top">
@@ -99,7 +99,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-9"></script>
     <script src="site-auth.js?v=20260524-9"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/admin.html b/frontend/admin.html
index ab017b3..94ce8d8 100644
--- a/frontend/admin.html
+++ b/frontend/admin.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
   </head>
   <body class="admin-page" data-page="admin">
     <nav class="navbar navbar-expand-lg sticky-top">
@@ -61,7 +61,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
                 <li class="d-none" id="navAdminMenuItem">
                   <span class="dropdown-item active" aria-current="page">Управление каталогом</span>
                 </li>
@@ -298,7 +297,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-10"></script>
     <script src="site-auth.js?v=20260524-10"></script>
-    <script src="admin.js?v=20260717-3"></script>
+    <script src="admin.js?v=20260717-4"></script>
     <script src="admin-page.js?v=20260524-10"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
diff --git a/frontend/admin.js b/frontend/admin.js
index f4acbdb..dec6e89 100644
--- a/frontend/admin.js
+++ b/frontend/admin.js
@@ -56,12 +56,21 @@ function _captureConsole(level, args) {
   });
   window.addEventListener("error", (e) => _captureConsole("error", [`${e.message} (${e.filename}:${e.lineno})`]));
   window.addEventListener("unhandledrejection", (e) => _captureConsole("error", [`Unhandled promise rejection: ${e.reason}`]));
+  // Стартовая строка — чтобы сразу было видно, что перехват консоли работает
+  console.log("Диагностическая консоль подключена: здесь видны сообщения console.log/warn/error этой вкладки.");
 })();
 
 function renderConsoleLogBuffer() {
   const el = $("#debugConsoleLog");
   if (!el) return;
   el.innerHTML = "";
+  if (!_consoleBuffer.length) {
+    const empty = document.createElement("div");
+    empty.className = "text-secondary";
+    empty.textContent = "Пока нет сообщений консоли (здесь появятся ошибки и предупреждения этой вкладки).";
+    el.appendChild(empty);
+    return;
+  }
   for (const entry of _consoleBuffer) {
     const cls = entry.level === "error" ? "text-danger" : entry.level === "warn" ? "text-warning" : "text-secondary";
     const line = document.createElement("div");
@@ -706,7 +715,9 @@ function renderBloomCard(title, bloom, colClass = "col-12") {
     return renderDebugCard(title, [["Статус", "нет данных"]]);
   }
   const canvasId = `bloomCanvas_${Math.random().toString(36).slice(2)}`;
-  const cols = Math.max(1, Math.round(Math.sqrt(bloom.m)));
+  // Более широкая сетка (не квадрат): при малом m занятые биты не выстраиваются
+  // в диагональную «лесенку», а распределяются по прямоугольнику читаемее
+  const cols = Math.max(8, Math.round(Math.sqrt(bloom.m) * 1.6));
   const rows = Math.ceil(bloom.m / cols);
   const cellSize = bloom.m <= 64 ? 20 : 8;
   _pendingBloomCanvases.push({ id: canvasId, bits: bloom.bits, cols, cellSize });
diff --git a/frontend/auth.html b/frontend/auth.html
index 0bc1298..f1dbf74 100644
--- a/frontend/auth.html
+++ b/frontend/auth.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
   </head>
   <body data-page="auth" class="auth-page">
     <nav class="navbar navbar-expand-lg sticky-top">
@@ -42,22 +42,15 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item dropdown d-none" id="navAuthUser">
               <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                 <span id="navAuthEmail" class="d-none d-md-inline">Аккаунт</span>
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -123,7 +116,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-4"></script>
     <script src="auth-page.js?v=20260524-4"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/compare.html b/frontend/compare.html
index 1cbf8d8..771b07b 100644
--- a/frontend/compare.html
+++ b/frontend/compare.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
 
 <!-- Yandex.Metrika counter -->
 <script type="text/javascript">
@@ -58,6 +58,9 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item" id="navAuthGuest">
               <a class="nav-link btn btn-link border-0 py-2 text-decoration-none" href="auth.html">Вход</a>
             </li>
@@ -67,16 +70,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -109,29 +102,74 @@
     <main class="container-fluid px-3 px-lg-4 py-4">
       <div id="alertBox" class="alert alert-danger d-none mb-3" role="alert"></div>
 
+      <!-- Рабочая точка прямо здесь: на практике её приходится задавать
+           повторно, глядя на сравниваемые кривые -->
       <div class="card shadow-sm border-0 mb-4" style="border-radius: 20px;">
         <div class="card-body p-4">
-          <h2 class="h6 fw-bold mb-3">Добавить модель для сравнения</h2>
-          <p class="small text-secondary mb-3">
-            Сначала выберите модельный ряд (аэродинамическую схему), затем — конкретный типоразмер внутри него,
-            как размер экрана у одного поколения телефона.
-          </p>
-          <form id="addCompareForm" class="row g-2 align-items-end">
-            <div class="col-12 col-md-5">
-              <label for="familySelect" class="form-label text-secondary small mb-1 fw-medium">Модельный ряд</label>
-              <select id="familySelect" class="form-select"></select>
+          <h2 class="h6 fw-bold mb-3">Рабочая точка на графике</h2>
+          <form id="comparePointForm" class="row g-2 align-items-end">
+            <div class="col-6 col-md-3">
+              <label for="comparePointQ" class="form-label text-secondary small mb-1 fw-medium">Расход Q, м³/ч</label>
+              <input id="comparePointQ" type="number" min="1" step="any" class="form-control" placeholder="м³/ч" />
             </div>
-            <div class="col-12 col-md-5">
-              <label for="variantSelect" class="form-label text-secondary small mb-1 fw-medium">Типоразмер</label>
-              <select id="variantSelect" class="form-select"></select>
+            <div class="col-6 col-md-3">
+              <label for="comparePointP" class="form-label text-secondary small mb-1 fw-medium">Давление P, Па</label>
+              <input id="comparePointP" type="number" min="1" step="any" class="form-control" placeholder="Па" />
             </div>
-            <div class="col-12 col-md-2">
-              <button type="submit" class="btn btn-dark w-100">Добавить</button>
+            <div class="col-6 col-md-2">
+              <label for="comparePointTol" class="form-label text-secondary small mb-1 fw-medium">Допуск, %</label>
+              <input id="comparePointTol" type="number" min="0" max="50" step="any" class="form-control" placeholder="± %" />
+            </div>
+            <div class="col-6 col-md-4 d-flex gap-2">
+              <button type="submit" class="btn btn-primary flex-grow-1">Показать точку</button>
+              <button type="button" id="comparePointResetBtn" class="btn btn-outline-secondary">Сброс</button>
             </div>
           </form>
         </div>
       </div>
 
+      <div class="card shadow-sm border-0 mb-4" style="border-radius: 20px;">
+        <div class="card-body p-4">
+          <h2 class="h6 fw-bold mb-3">Добавить модель для сравнения</h2>
+          <ul class="nav nav-pills nav-sm mb-3" role="tablist">
+            <li class="nav-item"><button class="nav-link active" data-bs-toggle="tab" data-bs-target="#addByFamily" type="button">По модельному ряду</button></li>
+            <li class="nav-item"><button class="nav-link" data-bs-toggle="tab" data-bs-target="#addBySearch" type="button">Поиск по таблице</button></li>
+          </ul>
+          <div class="tab-content">
+            <div class="tab-pane fade show active" id="addByFamily">
+              <form id="addCompareForm" class="row g-2 align-items-end">
+                <div class="col-12 col-md-5">
+                  <label for="familySelect" class="form-label text-secondary small mb-1 fw-medium">Модельный ряд</label>
+                  <select id="familySelect" class="form-select"></select>
+                </div>
+                <div class="col-12 col-md-5">
+                  <label for="variantSelect" class="form-label text-secondary small mb-1 fw-medium">Типоразмер</label>
+                  <select id="variantSelect" class="form-select"></select>
+                </div>
+                <div class="col-12 col-md-2">
+                  <button type="submit" class="btn btn-dark w-100">Добавить</button>
+                </div>
+              </form>
+            </div>
+            <div class="tab-pane fade" id="addBySearch">
+              <input id="compareSearchInput" class="form-control mb-2" type="search" placeholder="Поиск по модели, типу, размеру…" />
+              <div class="table-responsive" style="max-height: 320px; overflow-y: auto;">
+                <table class="table table-hover table-sm align-middle mb-0 compare-mini-table">
+                  <thead class="bg-light">
+                    <tr>
+                      <th>Модель</th><th>Тип</th><th class="text-end">Расход</th><th class="text-end">Давление</th><th class="text-end">Цена</th><th></th>
+                    </tr>
+                  </thead>
+                  <tbody id="compareSearchBody">
+                    <tr><td colspan="6" class="text-secondary text-center py-3">Введите запрос выше…</td></tr>
+                  </tbody>
+                </table>
+              </div>
+            </div>
+          </div>
+        </div>
+      </div>
+
       <div class="row g-4 align-items-stretch">
         <div class="col-12 col-xxl-6">
           <div class="card shadow-sm border-0 h-100" style="border-radius: 20px; overflow: hidden;">
@@ -150,7 +188,6 @@
           <div class="card shadow-sm border-0 h-100" style="border-radius: 20px; overflow: hidden;">
             <div class="card-header bg-white border-bottom-0 pt-4 px-4 pb-0 d-flex justify-content-between align-items-center flex-wrap gap-2">
               <h2 class="h5 fw-bold mb-0">Сводная таблица параметров</h2>
-              <span class="badge bg-success bg-opacity-10 text-success px-3 py-2" style="font-size: 0.85rem;">Зелёным выделен лучший показатель</span>
             </div>
             <div class="card-body p-0 mt-3" data-demo-guard>
               <div class="table-responsive">
@@ -173,7 +210,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-10"></script>
     <script src="site-auth.js?v=20260524-10"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/delivery.html b/frontend/delivery.html
index 1c1031d..0ca59b2 100644
--- a/frontend/delivery.html
+++ b/frontend/delivery.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
   </head>
   <body data-page="delivery">
     <nav class="navbar navbar-expand-lg sticky-top">
@@ -42,6 +42,9 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item" id="navAuthGuest">
               <button
                 class="nav-link btn btn-link border-0 py-2"
@@ -58,16 +61,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -180,7 +173,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-9"></script>
     <script src="site-auth.js?v=20260524-9"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/index.html b/frontend/index.html
index 585a2db..0eeedf3 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -14,7 +14,7 @@
       integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
         
 <!-- Yandex.Metrika counter -->
 <script type="text/javascript">
@@ -68,6 +68,9 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item" id="navAuthGuest">
               <button
                 class="nav-link btn btn-link border-0 py-2"
@@ -84,16 +87,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -391,7 +384,7 @@
     <script src="https://cdn.jsdelivr.net/npm/jspdf@2.5.1/dist/jspdf.umd.min.js"></script>
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-9"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-auth.js?v=20260524-9"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
diff --git a/frontend/product.html b/frontend/product.html
index c25b801..d946b03 100644
--- a/frontend/product.html
+++ b/frontend/product.html
@@ -14,7 +14,7 @@
       integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
     
     <!-- Yandex.Metrika counter -->
 <script type="text/javascript">
@@ -75,6 +75,9 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item" id="navAuthGuest">
               <a class="nav-link btn btn-link border-0 py-2 text-decoration-none" href="auth.html">Вход</a>
             </li>
@@ -84,16 +87,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -269,7 +262,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-10"></script>
     <script src="site-auth.js?v=20260524-10"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/project.html b/frontend/project.html
index 1df39a7..e602fe0 100644
--- a/frontend/project.html
+++ b/frontend/project.html
@@ -13,7 +13,7 @@
       rel="stylesheet"
       crossorigin="anonymous"
     />
-    <link rel="stylesheet" href="style.css?v=20260717-3" />
+    <link rel="stylesheet" href="style.css?v=20260717-4" />
 
 <!-- Yandex.Metrika counter -->
 <script type="text/javascript">
@@ -57,6 +57,9 @@
                 <span id="navCompareBadge" class="nav-compare-badge d-none">0</span>
               </a>
             </li>
+            <li class="nav-item d-none" id="navAdminMenuItem">
+              <a class="nav-link vs-admin-link" href="admin.html" target="_blank" rel="noopener noreferrer">⚙ Админ-панель</a>
+            </li>
             <li class="nav-item" id="navAuthGuest">
               <a class="nav-link btn btn-link border-0 py-2 text-decoration-none" href="auth.html">Вход</a>
             </li>
@@ -66,16 +69,6 @@
                 <span class="d-md-none">ЛК</span>
               </a>
               <ul class="dropdown-menu dropdown-menu-end position-absolute">
-                <li><a class="dropdown-item active" href="project.html">Мой проект</a></li>
-                <li class="d-none" id="navAdminMenuItem">
-                  <a
-                    class="dropdown-item"
-                    href="admin.html"
-                    target="_blank"
-                    rel="noopener noreferrer"
-                  >Управление каталогом</a>
-                </li>
-                <li><hr class="dropdown-divider" /></li>
                 <li><button class="dropdown-item" type="button" id="navLogoutBtn">Выйти</button></li>
               </ul>
             </li>
@@ -94,6 +87,10 @@
     </header>
 
     <main class="container-fluid px-3 px-lg-4 py-4 cabinet-page">
+      <div class="alert alert-warning d-flex align-items-center gap-2 mb-3" role="alert">
+        <span class="badge text-bg-warning">Инсайдерский режим</span>
+        <span class="small mb-0">Это черновой кабинет для подготовки спецификации клиенту (задать данные, водяной знак, скачать PDF). Отдельный кабинет сотрудника с прямой отправкой клиенту — в разработке.</span>
+      </div>
       <div id="alertBox" class="alert alert-danger d-none mb-3" role="alert"></div>
       <div id="successBox" class="alert alert-success d-none mb-3" role="alert"></div>
 
@@ -241,7 +238,7 @@
     <script src="config.js?v=20260424-1"></script>
     <script src="auth.js?v=20260524-4"></script>
     <script src="site-auth.js?v=20260524-4"></script>
-    <script src="script.js?v=20260717-3"></script>
+    <script src="script.js?v=20260717-4"></script>
     <script src="site-layout.js?v=20260709-1"></script>
   </body>
 </html>
diff --git a/frontend/script.js b/frontend/script.js
index da122f5..b78a34e 100644
--- a/frontend/script.js
+++ b/frontend/script.js
@@ -208,6 +208,27 @@ function sortItemsClient(items, sortKey) {
   });
 }
 
+// Скрытые колонки таблицы каталога (например, «Цена») — запоминаем выбор
+const CATALOG_HIDDEN_COLS_KEY = "ventsearch.catalog.hiddenCols";
+
+function loadHiddenCols() {
+  try {
+    const raw = localStorage.getItem(CATALOG_HIDDEN_COLS_KEY);
+    const arr = raw ? JSON.parse(raw) : [];
+    return new Set(Array.isArray(arr) ? arr.map(String) : []);
+  } catch {
+    return new Set();
+  }
+}
+
+function saveHiddenCols(set) {
+  try {
+    localStorage.setItem(CATALOG_HIDDEN_COLS_KEY, JSON.stringify([...set]));
+  } catch {
+    // приватный режим — не запоминаем
+  }
+}
+
 // Фильтры каталога переживают переход на карточку товара и возврат назад.
 // localStorage, а не sessionStorage: карточки часто открывают в НОВОЙ вкладке
 // (Ctrl+клик), а sessionStorage живёт только внутри одной вкладки — из-за
@@ -572,9 +593,13 @@ async function renderProductBlueprint(product) {
   }
 
   if (hint) {
-    hint.textContent =
-      `Чтобы заменить заглушки, положите в photos/ файлы blueprint_${slug}.png (чертёж) и ` +
-      `blueprintVals_${slug}.png (значения); подойдут и общие blueprint.png / blueprintVals.png.`;
+    // Стилизованная подпись только для администратора (тег [ДЕБАГ])
+    hint.classList.add("vs-debug-note");
+    hint.innerHTML =
+      `<span class="vs-debug-tag">[ДЕБАГ]</span> Файлы чертежа берутся из <code>photos/</code>: ` +
+      `<code>blueprint_${escapeHtml(slug)}.png</code> — сам чертёж, ` +
+      `<code>blueprintVals_${escapeHtml(slug)}.png</code> — таблица значений. ` +
+      `Подойдут и общие <code>blueprint.png</code> / <code>blueprintVals.png</code>.`;
   }
   revealAdminHintWhenAdmin(hint);
   card.classList.remove("d-none");
@@ -638,6 +663,18 @@ function shapePointsForModel(model) {
   return points ? enforceMonotonicDecreasing(points) : null;
 }
 
+// Прижимает «хвост» кривой (последние ~25% по расходу) к монотонному спаду,
+// чтобы у конца линии не было «отскока» вверх (артефакт параметрической кривой
+// у некоторых моделей). Седловину в середине (провал/горб осевых) не трогаем.
+function clampCurveTail(points) {
+  if (!Array.isArray(points) || points.length < 4) return points;
+  const start = Math.floor(points.length * 0.75);
+  for (let i = Math.max(1, start); i < points.length; i += 1) {
+    if (points[i][1] > points[i - 1][1]) points[i][1] = points[i - 1][1];
+  }
+  return points;
+}
+
 // Кусочно-линейная интерполяция по возрастающей сетке xs.
 function linearInterp(x, xs, ys) {
   if (x <= xs[0]) return ys[0];
@@ -795,6 +832,10 @@ function buildQpDatasetsShared(products, targetRpm = null, targetPoint = null, o
       const pScaled = Math.max(pNom * (scaleFactor * scaleFactor), 0);
       points.push([qScaled, pScaled]);
     }
+    // Хвост — строго вниз: убирает «отскок» у конца линии. Сами 200 точек уже
+    // лежат на аналитической кривой, поэтому дополнительное сглаживание
+    // ECharts (smooth) не нужно — оно и давало overshoot на концах.
+    clampCurveTail(points);
 
     const color = familyMode ? (isPrimary ? "#0d6efd" : "#c3ccd6") : colors[idx % colors.length];
     const siblingLabel = p.size || p.model || p.id;
@@ -805,11 +846,11 @@ function buildQpDatasetsShared(products, targetRpm = null, targetPoint = null, o
       // полное имя модели остаётся только у выделенной кривой
       name: curveName,
       type: 'line',
-      // smoothMonotone:'x' — монотонный сплайн: гладкая поверхность без
-      // «отскока» на концах (обычный smooth даёт overshoot у qMin/qMax)
-      smooth: true,
-      smoothMonotone: 'x',
+      smooth: false,
       symbol: 'none',
+      // Появление рисует линию от её начала (qMin) слева направо, а не с 0,0
+      animationDuration: 650,
+      animationEasing: 'cubicOut',
       data: points,
       lineStyle: { width: familyMode ? (isPrimary ? 4 : 1.25) : 3, color, opacity: familyMode && !isPrimary ? 0.85 : 1 },
       itemStyle: { color },
@@ -863,6 +904,7 @@ function buildQpDatasetsShared(products, targetRpm = null, targetPoint = null, o
     }
   });
 
+  let networkPoints = null;
   if (targetPoint && targetPoint.q > 0 && targetPoint.p > 0) {
     const k = targetPoint.p / Math.pow(targetPoint.q, 2);
     const maxQ = Math.max(
@@ -874,6 +916,7 @@ function buildQpDatasetsShared(products, targetRpm = null, targetPoint = null, o
       const q = (maxQ * i) / 100;
       systemPoints.push([q, k * Math.pow(q, 2)]);
     }
+    networkPoints = systemPoints;
 
     series.push({
       name: 'Кривая сети',
@@ -967,30 +1010,42 @@ function buildQpDatasetsShared(products, targetRpm = null, targetPoint = null, o
     }
   }
 
-  // Лёгкая анимация: полупрозрачный бегунок вдоль оси +Q (тип 'lines' с
-  // эффектом) — ненавязчивое «движение потока». Служебная серия guide-flow
-  // не попадает ни в легенду, ни в подсказку, ни в PDF.
-  const flowMaxQ = Math.max(0, ...productCurves.map((c) => c.points[c.points.length - 1][0]));
-  if (flowMaxQ > 0) {
+  // Лёгкая анимация: полупрозрачный бегунок ВДОЛЬ САМИХ КРИВЫХ (и кривой сети),
+  // а не по оси Q — «движение потока» по характеристике. Служебные серии
+  // guide-flow не попадают ни в легенду, ни в подсказку, ни в PDF.
+  function downsample(pts, n = 48) {
+    if (pts.length <= n) return pts.map((p) => [p[0], p[1]]);
+    const out = [];
+    for (let i = 0; i < n; i += 1) out.push(pts[Math.round((i * (pts.length - 1)) / (n - 1))]);
+    return out.map((p) => [p[0], p[1]]);
+  }
+  const flowSources = productCurves
+    .filter((c) => c.points.length >= 2)
+    .map((c) => ({ coords: downsample(c.points), color: c.color }));
+  if (networkPoints && networkPoints.length >= 2) {
+    flowSources.push({ coords: downsample(networkPoints), color: '#B45309' });
+  }
+  flowSources.forEach((src, i) => {
+    const rgb = src.color === '#B45309' ? '180, 83, 9' : '37, 99, 235';
     series.push({
-      name: 'guide-flow',
+      name: `guide-flow-${i}`,
       type: 'lines',
       coordinateSystem: 'cartesian2d',
       silent: true,
-      z: 0,
+      z: 6,
       polyline: true,
       effect: {
         show: true,
-        period: 6,
-        trailLength: 0.55,
-        symbol: 'arrow',
-        symbolSize: 5,
-        color: 'rgba(37, 99, 235, 0.28)',
+        period: 5,
+        trailLength: 0.4,
+        symbol: 'circle',
+        symbolSize: 4,
+        color: `rgba(${rgb}, 0.45)`,
       },
-      lineStyle: { color: 'rgba(37, 99, 235, 0)', width: 0, opacity: 0 },
-      data: [{ coords: [[0, 0], [flowMaxQ, 0]] }],
+      lineStyle: { color: `rgba(${rgb}, 0)`, width: 0, opacity: 0 },
+      data: [{ coords: src.coords }],
     });
-  }
+  });
 
   return series;
 }
@@ -1087,7 +1142,18 @@ function renderQpChartShared(container, chartRef, products, targetRpm = null, ta
     ],
     toolbox: {
       feature: {
-        dataZoom: { yAxisIndex: 'none', title: { zoom: 'Лупа', back: 'Сброс лупы' } },
+        // Встроенную «лупу с шаговым откатом» убрали: её back вёл себя как
+        // Ctrl+Z и не сбрасывал масштаб после зума мышью. Зум — колесом,
+        // а «myReset» возвращает СРАЗУ в исходное окно (по выбранной модели).
+        myReset: {
+          show: true,
+          title: 'Исходный масштаб',
+          icon: 'path://M512 128a384 384 0 1 0 384 384h-80a304 304 0 1 1-89-215l-79 79h248V107l-90 90A382 382 0 0 0 512 128z',
+          onclick: () => {
+            chartRef.dispatchAction({ type: 'dataZoom', xAxisIndex: 0, startValue: 0, endValue: xFocusEnd });
+            chartRef.dispatchAction({ type: 'dataZoom', yAxisIndex: 0, startValue: 0, endValue: yFocusEnd });
+          },
+        },
         saveAsImage: { title: 'Скачать PNG', name: 'ventsearch-chart' }
       },
       right: 40,
@@ -1309,12 +1375,17 @@ function ensurePdfMakerModal() {
                 placeholder="Например: Коммерческое предложение" />
             </div>
             <div class="col-md-6">
-              <label for="pdfMakerWatermark" class="form-label text-secondary small mb-1">
+              <label class="form-label text-secondary small mb-1">
                 Водяной знак компании (файл из папки photos/)
               </label>
-              <select id="pdfMakerWatermark" class="form-select">
-                <option value="">Без водяного знака</option>
-              </select>
+              <div class="dropdown w-100">
+                <button class="btn btn-outline-secondary dropdown-toggle w-100 d-flex align-items-center gap-2 text-start" type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside" id="pdfMakerWmBtn">
+                  <img id="pdfMakerWmThumb" alt="" style="width:26px;height:26px;object-fit:contain;display:none;border-radius:4px;background:#f1f5f9;">
+                  <span id="pdfMakerWmLabel" class="flex-grow-1 text-truncate">Без водяного знака</span>
+                </button>
+                <ul class="dropdown-menu w-100" id="pdfMakerWmMenu" style="max-height:300px;overflow-y:auto;"></ul>
+              </div>
+              <input type="hidden" id="pdfMakerWatermark" value="">
             </div>
           </div>
           <div class="d-flex gap-2 mt-3">
@@ -1412,7 +1483,6 @@ function ensurePdfMakerModal() {
     if (previewWrap.classList.contains("d-none")) return;
     void refreshPreview();
   }
-  modalEl.querySelector("#pdfMakerWatermark").addEventListener("change", autoRefreshIfPreviewOpen);
   modalEl.querySelector("#pdfMakerHeaderText").addEventListener("input", () => {
     if (previewWrap.classList.contains("d-none")) return;
     clearTimeout(headerDebounce);
@@ -1440,7 +1510,7 @@ function ensurePdfMakerModal() {
     previewWrap.classList.add("d-none");
   });
 
-  void populateWatermarkOptions(modalEl.querySelector("#pdfMakerWatermark"));
+  void populateWatermarkPicker(modalEl, autoRefreshIfPreviewOpen);
   return modalEl;
 }
 
@@ -1544,6 +1614,7 @@ async function initCatalogPage() {
     selectedIds: new Set(loadCompareIds()),
     projectIds: new Set(loadProjectIds()),
     analogs: [],
+    hiddenCols: loadHiddenCols(),
     // 'catalog' — обычный список (сортировка на сервере, есть пагинация);
     // 'point' — подбор по рабочей точке (все результаты уже в браузере,
     // клик по заголовку колонки сортирует их без запроса к серверу)
@@ -1680,17 +1751,40 @@ async function initCatalogPage() {
     // по модельному ряду. Один экран вмещает десятки моделей вместо 1-2
     // высоких карточек.
     const currentSort = String(sortSelect?.value || "");
+    // Кнопки действий встроены в колонку «Модель» (рядом с названием), а не
+    // отдельной колонкой справа — так понятнее, что кнопки относятся к строке.
     const COLUMNS = [
       { key: "model", label: "Модель", sortable: true },
-      { key: "size", label: "Типоразмер", sortable: false },
-      { key: "diameter", label: "⌀, мм", sortable: true, numeric: true },
-      { key: "airflow", label: "Расход Q, м³/ч", sortable: true, numeric: true },
-      { key: "pressure", label: "Давление P, Па", sortable: true, numeric: true },
-      { key: "power", label: "Мощность, Вт", sortable: true, numeric: true },
-      { key: "noise", label: "Шум, дБ", sortable: true, numeric: true },
-      { key: "price", label: "Цена", sortable: true, numeric: true },
-      { key: "_actions", label: "", sortable: false },
+      { key: "size", label: "Типоразмер", sortable: false, hideable: true },
+      { key: "diameter", label: "⌀, мм", sortable: true, numeric: true, hideable: true },
+      { key: "airflow", label: "Расход Q, м³/ч", sortable: true, numeric: true, hideable: true },
+      { key: "pressure", label: "Давление P, Па", sortable: true, numeric: true, hideable: true },
+      { key: "power", label: "Мощность, Вт", sortable: true, numeric: true, hideable: true },
+      { key: "noise", label: "Шум, дБ", sortable: true, numeric: true, hideable: true },
+      { key: "price", label: "Цена", sortable: true, numeric: true, hideable: true },
     ];
+    const hidden = state.hiddenCols;
+
+    // Панель «Колонки»: чекбоксы съёмных колонок — можно убрать ненужное
+    // (например, «Цену»). Выбор запоминается в localStorage.
+    const controls = document.createElement("div");
+    controls.className = "catalog-table-controls d-flex justify-content-end mb-2";
+    const dd = document.createElement("div");
+    dd.className = "dropdown";
+    dd.innerHTML = `
+      <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside">
+        Колонки
+      </button>
+      <ul class="dropdown-menu dropdown-menu-end p-2" style="min-width: 220px;">
+        ${COLUMNS.filter((c) => c.hideable).map((c) => `
+          <li>
+            <label class="dropdown-item d-flex align-items-center gap-2 mb-0">
+              <input type="checkbox" class="form-check-input mt-0 catalog-col-toggle" data-col="${c.key}" ${hidden.has(c.key) ? "" : "checked"}>
+              <span>${escapeHtml(c.label)}</span>
+            </label>
+          </li>`).join("")}
+      </ul>`;
+    controls.appendChild(dd);
 
     const wrap = document.createElement("div");
     wrap.className = "table-responsive catalog-table-wrap";
@@ -1702,6 +1796,8 @@ async function initCatalogPage() {
     for (const col of COLUMNS) {
       const th = document.createElement("th");
       th.textContent = col.label;
+      th.classList.add(`col-${col.key}`);
+      if (hidden.has(col.key)) th.classList.add("d-none");
       if (col.numeric) th.classList.add("text-end");
       if (col.sortable) {
         th.classList.add("catalog-sortable");
@@ -1737,6 +1833,11 @@ async function initCatalogPage() {
 
     const tbody = document.createElement("tbody");
 
+    function cellClass(col, extra = "") {
+      const base = `col-${col}${hidden.has(col) ? " d-none" : ""}`;
+      return extra ? `${base} ${extra}` : base;
+    }
+
     function buildProductRow(p) {
       state.cacheById.set(p.id, p);
       const selected = state.selectedIds.has(p.id);
@@ -1748,32 +1849,41 @@ async function initCatalogPage() {
         ? `<div class="mt-1"><span class="badge ${p._point.reserve_percent < 0 ? "text-bg-warning" : (p._point.reserve_percent <= 15 ? "text-bg-success" : "text-bg-secondary")}">В точке: ${escapeHtml(formatNumber(p._point.p_available))} Па · ${p._point.reserve_percent < 0 ? "дефицит " + escapeHtml(Math.abs(p._point.reserve_percent)) : "запас " + escapeHtml(p._point.reserve_percent)}%</span></div>`
         : "";
 
+      // Мини-иконка вентилятора (фото по типу) рядом с названием
+      const thumbUrls = getImageUrlCandidates(p);
+      const thumb = thumbUrls.length
+        ? `<img class="catalog-fan-thumb" src="${thumbUrls[0]}" alt="" loading="lazy" onerror="this.style.display='none'">`
+        : `<span class="catalog-fan-thumb catalog-fan-thumb-empty"></span>`;
+
       tr.innerHTML = `
-        <td class="catalog-cell-model">
-          <a href="product.html?id=${encodeURIComponent(p.id)}" class="fw-semibold">${escapeHtml(p.model || "Без названия")}</a>
-          ${pointBadge}
-        </td>
-        <td class="text-secondary">${escapeHtml(p.size || "—")}</td>
-        <td class="text-end">${p.diameter != null ? escapeHtml(formatNumber(p.diameter)) : "—"}</td>
-        <td class="text-end text-nowrap">${escapeHtml(p.airflow?.raw || "—")}</td>
-        <td class="text-end text-nowrap">${escapeHtml(p.pressure?.raw || "—")}</td>
-        <td class="text-end">${p.power != null ? escapeHtml(formatNumber(p.power)) : "—"}</td>
-        <td class="text-end">${p.noise_level != null ? escapeHtml(p.noise_level) : "—"}</td>
-        <td class="text-end text-nowrap fw-semibold">${escapeHtml(formatPrice(p.price))}</td>
-        <td class="text-end">
-          <div class="d-flex gap-1 justify-content-end">
-            <button type="button" class="btn btn-sm catalog-icon-btn btn-project-toggle ${inProject ? "btn-dark" : "btn-outline-dark"}"
-                    data-id="${escapeHtml(p.id)}" aria-label="Добавить в проект"
-                    title="${inProject ? "В проекте — нажмите, чтобы убрать" : "Добавить в проект"}">
-              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path><line x1="12" y1="11" x2="12" y2="17"></line><line x1="9" y1="14" x2="15" y2="14"></line></svg>
-            </button>
-            <button type="button" class="btn btn-sm catalog-icon-btn btn-compare-toggle ${selected ? "btn-dark" : "btn-outline-dark"}"
-                    data-id="${escapeHtml(p.id)}" aria-label="Добавить в сравнение"
-                    title="${selected ? "В сравнении — нажмите, чтобы убрать" : "Добавить в сравнение"}">
-              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"></polyline><path d="M3 11V9a4 4 0 0 1 4-4h14"></path><polyline points="7 23 3 19 7 15"></polyline><path d="M21 13v2a4 4 0 0 1-4 4H3"></path></svg>
-            </button>
+        <td class="${cellClass("model", "catalog-cell-model")}">
+          <div class="d-flex align-items-start gap-2">
+            ${thumb}
+            <div class="flex-grow-1" style="min-width: 0;">
+              <a href="product.html?id=${encodeURIComponent(p.id)}" class="fw-semibold">${escapeHtml(p.model || "Без названия")}</a>
+              ${pointBadge}
+              <div class="catalog-row-actions mt-1 d-flex gap-1">
+                <button type="button" class="btn btn-sm btn-project-toggle ${inProject ? "btn-dark" : "btn-outline-dark"}"
+                        data-id="${escapeHtml(p.id)}" title="${inProject ? "В проекте — нажмите, чтобы убрать" : "Добавить в проект"}">
+                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path><line x1="12" y1="11" x2="12" y2="17"></line><line x1="9" y1="14" x2="15" y2="14"></line></svg>
+                  <span>${inProject ? "В проекте" : "В проект"}</span>
+                </button>
+                <button type="button" class="btn btn-sm btn-compare-toggle ${selected ? "btn-dark" : "btn-outline-dark"}"
+                        data-id="${escapeHtml(p.id)}" title="${selected ? "В сравнении — нажмите, чтобы убрать" : "Добавить в сравнение"}">
+                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"></polyline><path d="M3 11V9a4 4 0 0 1 4-4h14"></path><polyline points="7 23 3 19 7 15"></polyline><path d="M21 13v2a4 4 0 0 1-4 4H3"></path></svg>
+                  <span>${selected ? "В сравнении" : "Сравнить"}</span>
+                </button>
+              </div>
+            </div>
           </div>
         </td>
+        <td class="${cellClass("size", "text-secondary")}">${escapeHtml(p.size || "—")}</td>
+        <td class="${cellClass("diameter", "text-end")}">${p.diameter != null ? escapeHtml(formatNumber(p.diameter)) : "—"}</td>
+        <td class="${cellClass("airflow", "text-end text-nowrap")}">${escapeHtml(p.airflow?.raw || "—")}</td>
+        <td class="${cellClass("pressure", "text-end text-nowrap")}">${escapeHtml(p.pressure?.raw || "—")}</td>
+        <td class="${cellClass("power", "text-end")}">${p.power != null ? escapeHtml(formatNumber(p.power)) : "—"}</td>
+        <td class="${cellClass("noise", "text-end")}">${p.noise_level != null ? escapeHtml(p.noise_level) : "—"}</td>
+        <td class="${cellClass("price", "text-end text-nowrap fw-semibold")}">${escapeHtml(formatPrice(p.price))}</td>
       `;
 
       tr.querySelector(".btn-project-toggle")?.addEventListener("click", (event) => {
@@ -1786,7 +1896,7 @@ async function initCatalogPage() {
         event.stopPropagation();
         toggleSelection(p.id);
       });
-      // Вся строка кликабельна (кроме кнопок) — открывает карточку модели
+      // Клик по строке (не по кнопке/ссылке) открывает карточку модели
       tr.addEventListener("click", (event) => {
         if (event.target.closest("button, a, select, input")) return;
         window.location.href = `product.html?id=${encodeURIComponent(p.id)}`;
@@ -1809,8 +1919,9 @@ async function initCatalogPage() {
         groupRow.className = "catalog-group-row";
         groupRow.innerHTML = `
           <td colspan="${COLUMNS.length}">
+            <span class="catalog-group-badge">РЯД</span>
             <span class="fw-bold">${escapeHtml(key || "Прочие")}</span>
-            <span class="text-secondary small ms-2">${items.length} ${pluralRu(items.length, "модель", "модели", "моделей")}</span>
+            <span class="text-secondary small ms-2">${items.length} ${pluralRu(items.length, "типоразмер", "типоразмера", "типоразмеров")}</span>
           </td>
         `;
         tbody.appendChild(groupRow);
@@ -1824,9 +1935,21 @@ async function initCatalogPage() {
     }
 
     table.appendChild(tbody);
+    grid.appendChild(controls);
     wrap.appendChild(table);
     grid.appendChild(wrap);
 
+    // Съём/возврат колонки: прячем все ячейки col-<key> и запоминаем выбор
+    controls.querySelectorAll(".catalog-col-toggle").forEach((cb) => {
+      cb.addEventListener("change", () => {
+        const key = cb.dataset.col;
+        if (cb.checked) state.hiddenCols.delete(key);
+        else state.hiddenCols.add(key);
+        saveHiddenCols(state.hiddenCols);
+        table.querySelectorAll(`.col-${key}`).forEach((el) => el.classList.toggle("d-none", !cb.checked));
+      });
+    });
+
     syncSelectionUi();
 
     if (paginationNav) {
@@ -2239,8 +2362,8 @@ async function initComparePage() {
           </button>
         </div>
         ${swapSelect}
-        <button type="button" class="btn btn-sm w-100 mt-1 btn-compare-add-project ${inProject ? "btn-dark" : "btn-outline-dark"}" data-id="${escapeHtml(p.id)}">
-          ${inProject ? "В проекте ✓" : "Добавить в проект"}
+        <button type="button" class="btn btn-sm w-100 mt-2 btn-compare-add-project ${inProject ? "in" : ""}" data-id="${escapeHtml(p.id)}">
+          ${inProject ? "✓ В проекте" : "＋ В проект"}
         </button>
       </th>`;
       })
@@ -2263,9 +2386,8 @@ async function initComparePage() {
       btn.addEventListener("click", () => {
         toggleProjectId(btn.dataset.id);
         const nowIn = isInProject(btn.dataset.id);
-        btn.classList.toggle("btn-dark", nowIn);
-        btn.classList.toggle("btn-outline-dark", !nowIn);
-        btn.textContent = nowIn ? "В проекте ✓" : "Добавить в проект";
+        btn.classList.toggle("in", nowIn);
+        btn.textContent = nowIn ? "✓ В проекте" : "＋ В проект";
       });
     });
 
@@ -2294,19 +2416,16 @@ async function initComparePage() {
       { label: "Цена, ₽", pick: (p) => toNumber(p.price), display: (p) => formatPrice(p.price), best: "min" },
     ];
 
+    // Зелёную подсветку «лучшего» значения убрали: у инженера нет правила
+    // «больше — лучше» (нужное давление/расход диктует задача, не максимум).
     for (const row of rows) {
       const values = products.map((p) => row.pick(p));
-      const valid = values.filter((v) => v != null);
-      let bestValue = null;
-      if (row.best === "max" && valid.length) bestValue = Math.max(...valid);
-      if (row.best === "min" && valid.length) bestValue = Math.min(...valid);
       const tr = document.createElement("tr");
       tr.innerHTML = `<td class="param-name">${escapeHtml(row.label)}</td>${products
         .map((p, idx) => {
           const raw = values[idx];
-          const isBest = bestValue != null && raw === bestValue;
           const text = row.display ? row.display(p) : raw ?? "—";
-          return `<td class="${isBest ? "best" : ""}">${escapeHtml(text)}</td>`;
+          return `<td>${escapeHtml(text)}</td>`;
         })
         .join("")}`;
       compareTableBody.appendChild(tr);
@@ -2330,6 +2449,97 @@ async function initComparePage() {
     }
   }
 
+  // Повторный ввод рабочей точки прямо на странице сравнения (Q,P[,допуск]) —
+  // сохраняем как общую рабочую точку и перерисовываем график с ней.
+  function initComparePointForm(products) {
+    const form = $("#comparePointForm");
+    const qEl = $("#comparePointQ");
+    const pEl = $("#comparePointP");
+    const tolEl = $("#comparePointTol");
+    const resetBtn = $("#comparePointResetBtn");
+    if (!form) return;
+    const stored = loadWorkingPoint();
+    if (stored) {
+      if (qEl) qEl.value = stored.q ?? "";
+      if (pEl) pEl.value = stored.p ?? "";
+      if (tolEl && stored.tol) tolEl.value = stored.tol;
+    }
+    form.addEventListener("submit", (e) => {
+      e.preventDefault();
+      const q = toNumber(qEl?.value);
+      const p = toNumber(pEl?.value);
+      const tol = Math.min(Math.max(toNumber(tolEl?.value) || 0, 0), 50);
+      if (!q || q <= 0 || !p || p <= 0) {
+        showError("Укажите расход Q и давление P (оба больше нуля).");
+        return;
+      }
+      hideError();
+      saveWorkingPoint({ q, p, tol: tol > 0 ? tol : undefined });
+      compareChart = renderQpChartShared(qpChartCanvas, compareChart, products, null, loadWorkingPoint());
+    });
+    resetBtn?.addEventListener("click", () => {
+      if (qEl) qEl.value = "";
+      if (pEl) pEl.value = "";
+      if (tolEl) tolEl.value = "";
+      saveWorkingPoint(null);
+      compareChart = renderQpChartShared(qpChartCanvas, compareChart, products, null, null);
+    });
+  }
+
+  // Мини-таблица поиска (как в каталоге) для добавления моделей в сравнение
+  function initCompareSearchTable() {
+    const input = $("#compareSearchInput");
+    const body = $("#compareSearchBody");
+    if (!input || !body) return;
+    let timer = null;
+    async function runSearch(q) {
+      const query = String(q || "").trim();
+      if (query.length < 2) {
+        body.innerHTML = `<tr><td colspan="6" class="text-secondary text-center py-3">Введите запрос (минимум 2 символа)…</td></tr>`;
+        return;
+      }
+      try {
+        const data = await fetchJson(apiUrl(`/api/products?q=${encodeURIComponent(query)}&limit=25&sort=price_asc`));
+        const items = Array.isArray(data?.items) ? data.items : [];
+        if (!items.length) {
+          body.innerHTML = `<tr><td colspan="6" class="text-secondary text-center py-3">Ничего не найдено.</td></tr>`;
+          return;
+        }
+        const compared = new Set(loadCompareIds());
+        body.innerHTML = items.map((p) => {
+          const inCompare = compared.has(String(p.id));
+          return `<tr>
+            <td><a href="product.html?id=${encodeURIComponent(p.id)}" class="fw-semibold">${escapeHtml(p.model || p.id)}</a></td>
+            <td class="text-secondary">${escapeHtml(p.type || "—")}</td>
+            <td class="text-end text-nowrap">${escapeHtml(p.airflow?.raw || "—")}</td>
+            <td class="text-end text-nowrap">${escapeHtml(p.pressure?.raw || "—")}</td>
+            <td class="text-end text-nowrap">${escapeHtml(formatPrice(p.price))}</td>
+            <td class="text-end"><button type="button" class="btn btn-sm ${inCompare ? "btn-dark" : "btn-primary"} compare-mini-add" data-id="${escapeHtml(p.id)}" ${inCompare ? "disabled" : ""}>${inCompare ? "✓" : "＋"}</button></td>
+          </tr>`;
+        }).join("");
+        body.querySelectorAll(".compare-mini-add").forEach((btn) => {
+          btn.addEventListener("click", () => {
+            const cur = new Set(loadCompareIds());
+            if (cur.size >= MAX_COMPARE && !cur.has(btn.dataset.id)) {
+              showError(`Максимум ${MAX_COMPARE} моделей одновременно.`);
+              return;
+            }
+            cur.add(btn.dataset.id);
+            saveCompareIds(cur);
+            window.location.reload();
+          });
+        });
+      } catch (err) {
+        console.error(err);
+        body.innerHTML = `<tr><td colspan="6" class="text-danger text-center py-3">Ошибка поиска.</td></tr>`;
+      }
+    }
+    input.addEventListener("input", () => {
+      clearTimeout(timer);
+      timer = setTimeout(() => runSearch(input.value), 300);
+    });
+  }
+
   // Каскадный подбор «модельный ряд → типоразмер» — как выбор поколения,
   // затем размера экрана у телефона, вместо плоского списка из 300+ моделей.
   const MAX_COMPARE = 6;
@@ -2392,6 +2602,7 @@ async function initComparePage() {
     hideError();
     await loadFamilies();
     initFamilyVariantPicker(loadCompareIds());
+    initCompareSearchTable();
     const ids = loadCompareIds();
     updateCompareNavBadge();
     if (ids.length < 1) {
@@ -2411,6 +2622,7 @@ async function initComparePage() {
     const products = await fetchProductsByIds(ids);
     renderCompareTable(products);
     renderCompareChart(products);
+    initComparePointForm(products);
 
     clearCompareBtn?.addEventListener("click", () => {
       saveCompareIds([]);
@@ -2548,9 +2760,13 @@ async function initProductPage() {
       const myFamily = families.find((f) => (f.variants || []).some((v) => String(v.id) === String(data.id)));
       if (myFamily && myFamily.variants.length > 1) familyVariants = myFamily.variants;
 
-      // Дропдаун ручного сравнения: группируем по модельному ряду —
-      // плоский список из 300+ моделей был бы бесполезен
-      for (const fam of families) {
+      // Дропдаун ручного сравнения: только модели ТОГО ЖЕ ТИПА (сравнивать
+      // осевой с центробежным бессмысленно), сгруппированные по модельному
+      // ряду — плоский список из 300+ моделей был бы бесполезен
+      const myType = normalizeType(data.type);
+      const sameType = families.filter((f) => normalizeType(f.type) === myType);
+      const famList = sameType.length ? sameType : families;
+      for (const fam of famList) {
         const items = (fam.variants || []).filter((x) => String(x.id) !== String(data.id));
         if (!items.length) continue;
         const group = document.createElement("optgroup");
@@ -2563,6 +2779,8 @@ async function initProductPage() {
         }
         compareWithSelect.appendChild(group);
       }
+      const firstOpt = compareWithSelect.querySelector("option[value='']");
+      if (firstOpt) firstOpt.textContent = sameType.length ? `Сравнить с моделью типа «${data.type}»...` : "Сравнить с другой моделью...";
     } catch (err) {
       console.error("families fetch failed", err);
     }
@@ -2696,6 +2914,60 @@ function buildProjectQuoteBody(products, profile, meta) {
 // пользователь выбирает водяной знак для PDF-документа клиенту.
 const WATERMARK_EXT_RE = /\.(png|jpe?g|webp|gif)$/i;
 
+// Логотип компании по умолчанию для водяного знака PDF
+const DEFAULT_WATERMARK = "ВЕНТМАШ_ЛОГО.png";
+
+function watermarkThumbUrl(name, versions) {
+  const v = versions?.[name];
+  return `${apiUrl(`/photos/${encodeURIComponent(name)}`)}${v ? `?v=${v}` : ""}`;
+}
+
+// Пикер водяного знака с миниатюрами логотипов (нативный <select> не умеет
+// показывать картинки). По умолчанию выбирается ВЕНТМАШ_ЛОГО.png, если есть.
+async function populateWatermarkPicker(modalEl, onChange) {
+  const menu = modalEl.querySelector("#pdfMakerWmMenu");
+  const hidden = modalEl.querySelector("#pdfMakerWatermark");
+  const label = modalEl.querySelector("#pdfMakerWmLabel");
+  const thumb = modalEl.querySelector("#pdfMakerWmThumb");
+  if (!menu || !hidden) return;
+
+  let versions = {};
+  try {
+    versions = await ensurePhotosVersionLoaded();
+  } catch (err) {
+    console.warn("Не удалось загрузить список файлов для водяного знака", err);
+  }
+  const names = Object.keys(versions || {}).filter((n) => WATERMARK_EXT_RE.test(n)).sort();
+
+  function select(name) {
+    hidden.value = name || "";
+    if (label) label.textContent = name || "Без водяного знака";
+    if (thumb) {
+      if (name) { thumb.src = watermarkThumbUrl(name, versions); thumb.style.display = ""; }
+      else { thumb.removeAttribute("src"); thumb.style.display = "none"; }
+    }
+    if (typeof onChange === "function") onChange();
+  }
+
+  const items = [{ name: "", labelText: "Без водяного знака" }]
+    .concat(names.map((n) => ({ name: n, labelText: n })));
+  menu.innerHTML = items.map((it) => `
+    <li>
+      <button type="button" class="dropdown-item d-flex align-items-center gap-2 pdf-wm-item" data-name="${escapeHtml(it.name)}">
+        ${it.name ? `<img src="${watermarkThumbUrl(it.name, versions)}" alt="" style="width:26px;height:26px;object-fit:contain;border-radius:4px;background:#f1f5f9;">` : `<span style="width:26px;height:26px;display:inline-block;"></span>`}
+        <span class="text-truncate">${escapeHtml(it.labelText)}</span>
+      </button>
+    </li>`).join("");
+  menu.querySelectorAll(".pdf-wm-item").forEach((btn) => {
+    btn.addEventListener("click", () => select(btn.dataset.name));
+  });
+
+  // По умолчанию — логотип ВЕНТМАШ, если он лежит в photos/
+  select(names.includes(DEFAULT_WATERMARK) ? DEFAULT_WATERMARK : "");
+}
+
+// Совместимость: старый вызов для нативного select (страница проекта больше
+// не использует, но оставляем на случай других мест)
 async function populateWatermarkOptions(selectEl) {
   if (!selectEl) return;
   try {
@@ -3075,6 +3347,18 @@ function initShareLinkButton() {
   });
 }
 
+// Возврат «Назад»/«Вперёд» отдаёт страницу из bfcache браузера БЕЗ повторного
+// запуска скриптов: выделения сравнения/проекта и фильтры остаются от прошлого
+// показа, хотя в localStorage они уже изменены на другой вкладке. Форсируем
+// перезагрузку, чтобы состояние всегда совпадало с localStorage.
+window.addEventListener("pageshow", (event) => {
+  const nav = performance.getEntriesByType?.("navigation")?.[0];
+  const isBackForward = nav ? nav.type === "back_forward" : false;
+  if (event.persisted || isBackForward) {
+    window.location.reload();
+  }
+});
+
 document.addEventListener("DOMContentLoaded", () => {
   applyDemoModeGuardIfNeeded();
   applyLockedSections();
diff --git a/frontend/style.css b/frontend/style.css
index 4601b8a..bb389bb 100644
--- a/frontend/style.css
+++ b/frontend/style.css
@@ -1561,3 +1561,134 @@ body[data-page="product"] .tabs-card .card-body {
   margin-bottom: 0.25rem;
   display: block;
 }
+
+/* ============================================================
+   Каталог v2: мини-иконки, кнопки у названия, явные группы,
+   съёмные колонки
+   ============================================================ */
+
+.catalog-fan-thumb {
+  width: 40px;
+  height: 40px;
+  object-fit: contain;
+  border-radius: 8px;
+  background: var(--vs-bg);
+  border: 1px solid var(--vs-border);
+  flex-shrink: 0;
+  padding: 2px;
+}
+
+.catalog-fan-thumb-empty {
+  display: inline-block;
+}
+
+.catalog-row-actions .btn {
+  --bs-btn-padding-y: 0.2rem;
+  --bs-btn-padding-x: 0.5rem;
+  --bs-btn-font-size: 0.76rem;
+  width: auto;
+  padding: 0.2rem 0.5rem;
+  font-size: 0.76rem;
+  display: inline-flex;
+  align-items: center;
+  gap: 0.3em;
+  white-space: nowrap;
+}
+
+.catalog-row-actions .btn.btn-dark {
+  background: var(--vs-accent);
+  color: #fff;
+}
+
+.catalog-row-actions .btn.btn-outline-dark {
+  background: var(--vs-surface);
+  border: 1px solid var(--vs-border);
+  color: var(--vs-text);
+}
+
+/* Строка-заголовок модельного ряда — явно отличается от строк вентиляторов:
+   акцентная полоса слева, заливка, чуть выше */
+.catalog-group-row td {
+  background: #e7eefb;
+  border-top: 2px solid var(--vs-accent);
+  border-bottom: 1px solid #cdd9ef;
+  padding: 0.6rem 0.9rem;
+}
+
+.catalog-group-badge {
+  display: inline-block;
+  background: var(--vs-accent);
+  color: #fff;
+  font-size: 0.64rem;
+  font-weight: 800;
+  letter-spacing: 0.06em;
+  padding: 0.1rem 0.45rem;
+  border-radius: 5px;
+  margin-right: 0.5rem;
+  vertical-align: middle;
+}
+
+.catalog-table-controls .dropdown-menu {
+  font-size: 0.9rem;
+}
+
+/* Кнопка «в проект» в шапке сравнения — заметный фон (не блёклый outline) */
+.btn-compare-add-project {
+  background: #eef4ff;
+  border: 1px solid var(--vs-accent);
+  color: var(--vs-accent);
+  font-weight: 700;
+}
+
+.btn-compare-add-project:hover {
+  background: #dfeaff;
+  color: var(--vs-accent-hover);
+}
+
+.btn-compare-add-project.in {
+  background: var(--vs-accent);
+  color: #fff;
+  border-color: var(--vs-accent);
+}
+
+/* Кнопка «Админ-панель» в верхней навигации — отдельным цветом (янтарь),
+   чтобы явно отличалась от обычных пунктов меню */
+.vs-admin-link {
+  background: #b45309 !important;
+  color: #fff !important;
+  font-weight: 700;
+}
+
+.vs-admin-link:hover {
+  background: #92400e !important;
+  color: #fff !important;
+}
+
+/* Служебные [ДЕБАГ]-подписи (видны только администратору) */
+.vs-debug-note {
+  background: #fff7ed;
+  border: 1px dashed #fdba74;
+  border-radius: 8px;
+  padding: 0.5rem 0.75rem;
+  color: #7c2d12;
+  font-size: 0.82rem;
+}
+
+.vs-debug-note code {
+  background: #ffedd5;
+  color: #9a3412;
+  padding: 0 0.25rem;
+  border-radius: 4px;
+}
+
+.vs-debug-tag {
+  display: inline-block;
+  background: #b45309;
+  color: #fff;
+  font-weight: 800;
+  font-size: 0.68rem;
+  letter-spacing: 0.05em;
+  padding: 0.05rem 0.4rem;
+  border-radius: 4px;
+  margin-right: 0.35rem;
+}
